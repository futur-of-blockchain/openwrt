diff --git a/kernel/mm-board-config/Makefile b/kernel/mm-board-config/Makefile
index d62a7ac..d96a0c1 100644
--- a/kernel/mm-board-config/Makefile
+++ b/kernel/mm-board-config/Makefile
@@ -13,11 +13,11 @@ include $(TOPDIR)/rules.mk
 PKG_NAME:=morse-board-config
 PKG_RELEASE=6
 
-PKG_VERSION:=rel_1_15_3_2025_Apr_16
+PKG_VERSION:=rel_1_14_1_2024_Dec_05
 
-PKG_SOURCE:=morsemicro-bcf-rel_1_15_3_2025_Apr_16.tar
-PKG_SOURCE_URL:=https://github.com/MorseMicro/bcf_binaries/releases/download/v1.15.3/
-PKG_HASH:=bd794c829b57a9049b67bdc9ec9015c5f51ebf7716836e6a37a7962fb5121ef3
+PKG_SOURCE:=morsemicro-bcf-rel_1_14_1_2024_Dec_05.tar
+PKG_SOURCE_URL:=https://github.com/MorseMicro/bcf_binaries/releases/download/v1.14.1/
+PKG_HASH:=ea90cfa1b7d6111c681bde013e51f88d54274548704ffdf14f6ddaa555150144
 
 PKG_MAINTAINER:=Morse Micro <info@morsemicro.com>
 PKG_BUILD_PARALLEL:=1
diff --git a/netifd-morse/lib/wifi/morse.sh b/netifd-morse/lib/wifi/morse.sh
index b24ad01..2f314c0 100755
--- a/netifd-morse/lib/wifi/morse.sh
+++ b/netifd-morse/lib/wifi/morse.sh
@@ -122,42 +122,41 @@ EOF
 
 		board=$(board_name)
 
-		# board_type is 'we have OTP bits set', in which case it should
-		# automatically load the correct file (bcf_boardtype...) and
-		# we don't need to override.
-		# We force Artini since 4v3 support currently requires 
-		# an explicit BCF file, though currently the AZW modules
-		# do not have OTP bits burnt.
-		if [ "$board_type" -eq 0 ] || [ "$board" = morse,artini ]; then
-			case "$board" in
-				morse,ekh01-03 |\
-				morse,ekh03v3)
-					bcf=bcf_mf08551.bin
-				;;
-				morse,ekh01v1)
-					bcf=bcf_mf03120.bin
-				;;
-				morse,ekh01v2)
-					bcf=bcf_mf08251.bin
-				;;
-				morse,ekh04v6)
-					bcf=bcf_ekh04_v6.bin
-				;;
-				morse,artini)
-					bcf=bcf_mm_hl1.bin
-				;;
-				morse,ekh01-01)
-					bcf=bcf_mf15457.bin
-				;;
-				*)
-					if [[ $path  = *usb* ]]; then
-						bcf=bcf_mf15457.bin
-					fi
-				;;
-			esac
-
-			[ -n "${bcf}" ] && uci -q set wireless.radio${devidx}.bcf="${bcf}"
-		fi
+	    ################################################################
+	    # Post World project, Most suitable BCF for the MM610X-H06
+	    #
+	    #if [ "$board_type" -eq 0 ] || [ "$board" = morse,artini ]; then
+	    #       case "$board" in
+	    #               morse,ekh01-03 |\
+	    #               morse,ekh03v3)
+	    #                       bcf=bcf_mf08551.bin
+	    #               ;;
+	    #               morse,ekh01v1)
+	    #                       bcf=bcf_mf03120.bin
+	    #               ;;
+	    #               morse,ekh01v2)
+        #                       bcf=bcf_mf08251.bin
+	    #               ;;
+	    #               morse,ekh04v6)
+	    #                       bcf=bcf_ekh04_v6.bin
+	    #               ;;
+	    #               morse,artini)
+	    #                       bcf=bcf_mm_hl1.bin
+	    #               ;;
+	    #               morse,ekh01-01)
+	    #                       bcf=bcf_mf15457.bin
+	    #               ;;
+	    #               *)
+	    #                       if [[ $path  = *usb* ]]; then
+	    #                               bcf=bcf_mf15457.bin
+	    #                       fi
+	    #               ;;
+	    #       esac
+	    #
+	    #       [ -n "${bcf}" ] && uci -q set wireless.radio${devidx}.bcf="${bcf}"
+	    #fi
+
+	    uci -q set wireless.radio${devidx}.bcf="bcf_mf10220.bin"
 
 		uci -q commit wireless
 
